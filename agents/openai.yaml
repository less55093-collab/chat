interface:
  display_name: "Chat"
  short_description: "Read-only code-aware discovery for unclear ideas"
  default_prompt: "Use $chat to discuss my rough project idea in read-only mode, inspect the codebase, ask targeted follow-up questions, compare tradeoffs, and shape a confirmation-ready plan draft without changing files, creating prototypes, installing dependencies, committing, or pushing."
