interface:
  display_name: "Work"
  short_description: "State-machine execution with delegated tasks"
  default_prompt: "Use $work after I have explicitly approved a $chat plan. Create or update root WORK_STATE.md, execute one atomic task at a time, assign each active task to one child agent with allowed read/write scopes, handle file access requests, run main-agent verification, update state transitions, and finish with whole-change validation."
